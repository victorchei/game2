# AI Sandbox Prototype

Команди приклади (English):

- move north 3
- mine east
- place south
- scan
- build tower
- go to 15 12
- inventory

Voice: натиснути 🎤 і сказати команду англійською.

## Гра на GitHub Pages

Ця гра вже налаштована для роботи на GitHub Pages! 

### Автоматичний деплой

Гра автоматично публікується на GitHub Pages при кожному push в гілку `main`. 
GitHub Actions workflow автоматично виконає деплой.

### Як активувати GitHub Pages:

1. У репозиторії перейдіть до **Settings** → **Pages**
2. У розділі **Build and deployment**:
   - Source: оберіть **GitHub Actions**
3. Гра буде доступна за адресою: `https://victorchei.github.io/game2/`

### Якщо створюєте новий репозиторій:

1. Створіть новий репозиторій на GitHub
2. Додайте файли:
   ```bash
   git init
   git add .
   git commit -m "init game"
   git branch -M main
   git remote add origin <your_repo_url>
   git push -u origin main
   ```
3. У Settings → Pages оберіть Source: **GitHub Actions**
4. Через ~1 хвилину гра буде доступна за `https://<username>.github.io/<repo>/`

Покращення (можна додати пізніше):

- Збереження світу в localStorage.
- Більше типів блоків.
- Взаємодія з реальним LLM API (парсинг складніших інструкцій).
- Анімації та zoom.
